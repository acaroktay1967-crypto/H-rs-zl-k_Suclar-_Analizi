<Window x:Class="HirsizlikSuclariAnalizi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HirsizlikSuclariAnalizi"
        mc:Ignorable="d"
        Title="Hırsızlık Suçları Analizi - TCK 141-142" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="20">
            <StackPanel>
                <TextBlock Text="Hırsızlık Suçları Analiz Sistemi" 
                           FontSize="28" FontWeight="Bold" Foreground="White" 
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Türk Ceza Kanunu 141-142. Maddeler" 
                           FontSize="14" Foreground="#BDC3C7" 
                           HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Navigation Menu -->
            <Border Grid.Column="0" Background="#34495E" BorderBrush="#2C3E50" BorderThickness="0,0,2,0">
                <StackPanel Margin="0,20,0,0">
                    <Button x:Name="btnCezaHesaplama" Content="📊 Ceza Hesaplama" 
                            Click="BtnCezaHesaplama_Click"
                            Style="{StaticResource MenuButtonStyle}"
                            Margin="10,5"/>
                    <Button x:Name="btnYargitayKararlari" Content="⚖️ Yargıtay Kararları" 
                            Click="BtnYargitayKararlari_Click"
                            Style="{StaticResource MenuButtonStyle}"
                            Margin="10,5"/>
                    <Button x:Name="btnHakkinda" Content="ℹ️ Hakkında" 
                            Click="BtnHakkinda_Click"
                            Style="{StaticResource MenuButtonStyle}"
                            Margin="10,5"/>
                </StackPanel>
            </Border>

            <!-- Content Area -->
            <Border Grid.Column="1" Background="#ECF0F1">
                <ContentControl x:Name="contentArea"/>
            </Border>
        </Grid>
    </Grid>

    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2C3E50"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
</Window>
